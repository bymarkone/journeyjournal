{
  "name": "journeyjournal",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:bundle && npm run build:static",
    "build:bundle": "webpack --devtool source-map --cash --colors --progress",
    "build:static": "cp app/index.html dist/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^0.13.3",
    "webpack": "^1.12.0"
  },
  "devDependencies": {
    "jsx-loader": "^0.13.2"
  }
}
