{
  "name": "journeyjournal",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:less && npm run build:bundle && npm run build:static",
    "build:less": "lessc app/stylesheets/index.less dist/stylesheets/index.css",
    "build:bundle": "webpack --devtool source-map --cash --colors --progress",
    "build:static": "cp app/index.html dist/index.html",
    "serve-dev": "webpack-dev-server --colors --progress --content-base dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "codemirror": "^5.6.0",
    "react": "^0.13.3",
    "react-codemirror": "^0.1.3",
    "webpack": "^1.12.0"
  },
  "devDependencies": {
    "css-loader": "^0.16.0",
    "jsx-loader": "^0.13.2",
    "less": "^2.5.1",
    "style-loader": "^0.12.3",
    "webpack-dev-server": "^1.10.1"
  }
}
